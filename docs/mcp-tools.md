# MCP tool reference

Use the [consumer agent contract](consumer-tools.md) for the active 16-tool server: reads, cart edits, checkout preview, placement and operation/payment status.

The [legacy Drive tool](drive-mcp-tools.md) belongs to `src/index.ts` and its HTTP/stdio runtime, not `src/consumer/index.ts`.
